# Remark42 Configuration
# Copy this file to .env and fill in your values

# ===== REQUIRED =====

# Remark42 Secret (generate with: openssl rand -hex 32)
REMARK_SECRET=your_secret_here_generate_with_openssl_rand

# Admin Configuration
# After first login, get your ID from browser console and update this
ADMIN_SHARED_ID=admin
ADMIN_EMAIL=your-email@example.com

# ===== SMTP CONFIGURATION (Optional but recommended) =====

# Gmail Example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your_gmail_app_password

# Strato Example:
# SMTP_HOST=smtp.strato.de
# SMTP_PORT=465
# SMTP_USERNAME=your-email@strato.de
# SMTP_PASSWORD=your_password

# Hetzner Example:
# SMTP_HOST=mail.your-server.de
# SMTP_PORT=587
# SMTP_USERNAME=your-email@your-domain.de
# SMTP_PASSWORD=your_password

# ===== NOTES =====

# How to generate REMARK_SECRET:
# Run on your server: openssl rand -hex 32

# How to get ADMIN_SHARED_ID:
# 1. Deploy Remark42
# 2. Visit https://comments.zimtkeksundapfeltarte.com/web/
# 3. Login with Google/GitHub/etc.
# 4. Open browser console (F12)
# 5. Type: localStorage.getItem('remark42-user')
# 6. Copy the "id" field value
# 7. Update this .env file
# 8. Restart: docker compose restart remark42

# Gmail App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2FA if not already
# 3. Go to App Passwords
# 4. Generate password for "Mail"
# 5. Use this password in SMTP_PASSWORD
