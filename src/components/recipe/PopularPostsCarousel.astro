---
/**
 * Popular Posts Carousel Component
 * Based on RelatedPostsCarousel template
 * Shows most popular recipes (TODO: Replace with real view data from API)
 */

// Mock data for popular recipes (TODO: Replace with real API call)
const mockPopularRecipes = [
  {
    slug: 'omas-plaetzchen',
    title: 'Omas Plätzchen',
    image: '/src/content/blog/images/omas-plaetzchen/omas-plaetzchen-featured.jpg',
    totalTime: 'PT45M',
    cuisine: 'Deutsch',
    servings: 24,
    difficulty: 'einfach'
  },
  {
    slug: 'fake-spaghetti-carbonara',
    title: 'Spaghetti Carbonara',
    image: '/src/content/blog/images/fake-spaghetti-carbonara/fake-spaghetti-carbonara-featured.jpg',
    totalTime: 'PT25M',
    cuisine: 'Italienisch',
    servings: 4,
    difficulty: 'mittel'
  },
  {
    slug: 'fake-lasagne-bolognese',
    title: 'Lasagne Bolognese',
    image: '/src/content/blog/images/fake-lasagne-bolognese/fake-lasagne-bolognese-featured.jpg',
    totalTime: 'PT90M',
    cuisine: 'Italienisch',
    servings: 6,
    difficulty: 'mittel'
  },
  {
    slug: 'fake-pizza-margherita',
    title: 'Pizza Margherita',
    image: '/src/content/blog/images/fake-pizza-margherita/fake-pizza-margherita-featured.jpg',
    totalTime: 'PT30M',
    cuisine: 'Italienisch',
    servings: 2,
    difficulty: 'einfach'
  },
  {
    slug: 'fake-schnitzel',
    title: 'Wiener Schnitzel',
    image: '/src/content/blog/images/fake-schnitzel/fake-schnitzel-featured.jpg',
    totalTime: 'PT30M',
    cuisine: 'Österreichisch',
    servings: 4,
    difficulty: 'mittel'
  },
  {
    slug: 'fake-tiramisu',
    title: 'Tiramisu',
    image: '/src/content/blog/images/fake-tiramisu/fake-tiramisu-featured.jpg',
    totalTime: 'PT30M',
    cuisine: 'Italienisch',
    servings: 8,
    difficulty: 'einfach'
  },
  {
    slug: 'fake-paella',
    title: 'Paella Valenciana',
    image: '/src/content/blog/images/fake-paella/fake-paella-featured.jpg',
    totalTime: 'PT60M',
    cuisine: 'Spanisch',
    servings: 6,
    difficulty: 'schwer'
  },
  {
    slug: 'fake-risotto-milanese',
    title: 'Risotto Milanese',
    image: '/src/content/blog/images/fake-risotto-milanese/fake-risotto-milanese-featured.jpg',
    totalTime: 'PT35M',
    cuisine: 'Italienisch',
    servings: 4,
    difficulty: 'mittel'
  },
  {
    slug: 'fake-gnocchi',
    title: 'Gnocchi mit Tomatensauce',
    image: '/src/content/blog/images/fake-gnocchi/fake-gnocchi-featured.jpg',
    totalTime: 'PT60M',
    cuisine: 'Italienisch',
    servings: 4,
    difficulty: 'mittel'
  },
  {
    slug: 'doener-aus-dem-ofen',
    title: 'Döner aus dem Ofen',
    image: '/src/content/blog/images/doener-aus-dem-ofen/doener-aus-dem-ofen-featured.jpg',
    totalTime: 'PT120M',
    cuisine: 'Türkisch',
    servings: 4,
    difficulty: 'mittel'
  },
  {
    slug: 'fake-pancakes',
    title: 'Amerikanische Pancakes',
    image: '/src/content/blog/images/fake-pancakes/fake-pancakes-featured.jpg',
    totalTime: 'PT20M',
    cuisine: 'Amerikanisch',
    servings: 4,
    difficulty: 'einfach'
  },
  {
    slug: 'fake-sushi-rolls',
    title: 'Sushi Rolls',
    image: '/src/content/blog/images/fake-sushi-rolls/fake-sushi-rolls-featured.jpg',
    totalTime: 'PT60M',
    cuisine: 'Japanisch',
    servings: 4,
    difficulty: 'schwer'
  },
];

const posts = mockPopularRecipes.slice(0, 12);

// Convert ISO 8601 duration to minutes
function parseDuration(duration?: string): number | null {
  if (!duration) return null;
  const match = duration.match(/PT(\d+)M/);
  return match ? parseInt(match[1]) : null;
}

// Map difficulty to display text
const difficultyMap: Record<string, string> = {
  einfach: "Einfach",
  mittel: "Mittel",
  schwer: "Fortgeschritten",
};

// Schema.org ItemList für SEO
const itemListSchema = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Meist besuchte Rezepte",
  "itemListElement": posts.map((post, index) => ({
    "@type": "ListItem",
    "position": index + 1,
    "url": `https://die-mama-kocht.de/rezepte/${post.slug}`,
    "name": post.title,
  })),
};
---

{posts.length > 0 && (
  <section
    class="popular-posts"
    aria-labelledby="popular-posts-heading"
  >
    <h2 id="popular-posts-heading" class="popular-posts__heading">
      Meist besuchte Rezepte
    </h2>

    <div class="popular-posts__carousel-container">
      <!-- Previous Button -->
      <button
        type="button"
        class="popular-posts__nav-button popular-posts__nav-button--prev"
        aria-label="Vorherige beliebte Rezepte anzeigen"
        data-carousel-prev
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="popular-posts__nav-icon"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M15 19l-7-7 7-7"
          />
        </svg>
      </button>

      <!-- Carousel Wrapper -->
      <div class="popular-posts__carousel-wrapper">
        <!-- Carousel Track -->
        <div
          class="popular-posts__carousel"
          role="list"
          aria-label="Liste beliebter Rezepte"
          data-carousel-track
        >
          {posts.map((post) => {
            const featuredImage = post.image || 'https://picsum.photos/400/300';
            const altText = post.title;
            const totalMinutes = parseDuration(post.totalTime);
            const cuisine = post.cuisine;
            const servings = post.servings;
            const difficulty = post.difficulty;

            return (
              <article
                class="popular-posts__card"
                role="listitem"
              >
                <a
                  href={`/rezepte/${post.slug}`}
                  class="popular-posts__card-link"
                >
                  <div class="popular-posts__card-image-wrapper">
                    <img
                      src={featuredImage}
                      alt={altText}
                      class="popular-posts__card-image"
                      loading="lazy"
                      width="400"
                      height="300"
                    />
                  </div>

                  <div class="popular-posts__card-content">
                    <h3 class="popular-posts__card-title">
                      {post.title}
                    </h3>

                    {/* Meta-Informationen */}
                    <div class="popular-posts__card-meta-grid">
                      {totalMinutes && (
                        <div class="popular-posts__card-meta-item">
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="popular-posts__card-icon"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                            aria-hidden="true"
                          >
                            <path
                              stroke-linecap="round"
                              stroke-linejoin="round"
                              stroke-width="2"
                              d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                            />
                          </svg>
                          <span class="popular-posts__card-meta-text">{totalMinutes} min</span>
                        </div>
                      )}

                      {cuisine && (
                        <div class="popular-posts__card-meta-item">
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="popular-posts__card-icon"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                            aria-hidden="true"
                          >
                            <path
                              stroke-linecap="round"
                              stroke-linejoin="round"
                              stroke-width="2"
                              d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
                            />
                          </svg>
                          <span class="popular-posts__card-meta-text">{cuisine}</span>
                        </div>
                      )}

                      {servings && (
                        <div class="popular-posts__card-meta-item">
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="popular-posts__card-icon"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                            aria-hidden="true"
                          >
                            <path
                              stroke-linecap="round"
                              stroke-linejoin="round"
                              stroke-width="2"
                              d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"
                            />
                          </svg>
                          <span class="popular-posts__card-meta-text">{servings}</span>
                        </div>
                      )}

                      {difficulty && (
                        <div class="popular-posts__card-meta-item">
                          <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="popular-posts__card-icon"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                            aria-hidden="true"
                          >
                            <path
                              stroke-linecap="round"
                              stroke-linejoin="round"
                              stroke-width="2"
                              d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                            />
                          </svg>
                          <span class="popular-posts__card-meta-text">{difficultyMap[difficulty] || difficulty}</span>
                        </div>
                      )}
                    </div>
                  </div>
                </a>
              </article>
            );
          })}
        </div>
      </div>

      <!-- Next Button -->
      <button
        type="button"
        class="popular-posts__nav-button popular-posts__nav-button--next"
        aria-label="Nächste beliebte Rezepte anzeigen"
        data-carousel-next
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="popular-posts__nav-icon"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M9 5l7 7-7 7"
          />
        </svg>
      </button>

      <!-- Accessibility: Info für Screen Reader -->
      <div class="sr-only" role="status" aria-live="polite" data-carousel-status>
        Zeige 3 von {posts.length} beliebten Rezepten.
      </div>
    </div>

    <!-- Schema.org JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify(itemListSchema)} />
  </section>
)}

<style>
  /* ==========================================
     POPULAR POSTS CAROUSEL - MANUAL CONTROL
     ========================================== */

  .popular-posts {
    container-type: inline-size;
    container-name: carousel;
    margin-block: 3rem;
  }

  .popular-posts__heading {
    font-size: 1.875rem;
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: 1.5rem;
    padding-bottom: 0.75rem;
    border-bottom: 3px solid var(--color-accent);
  }

  /* Carousel Container with Navigation */
  .popular-posts__carousel-container {
    position: relative;
  }

  /* Navigation Buttons - Overlay */
  .popular-posts__nav-button {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 2.5rem;
    height: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-accent);
    border: none;
    border-radius: 50%;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 10;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  }

  .popular-posts__nav-button--prev {
    left: -1rem;
  }

  .popular-posts__nav-button--next {
    right: -1rem;
  }

  .popular-posts__nav-button:hover:not(:disabled) {
    background: hsla(214, 41%, 10%, 1.00);
    transform: translateY(-50%) scale(1.1);
  }

  .popular-posts__nav-button:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }

  .popular-posts__nav-button:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  .popular-posts__nav-icon {
    width: 1.25rem;
    height: 1.25rem;
    color: white;
    transition: color 0.3s ease;
  }

  /* Carousel Wrapper - muss volle Container-Breite haben */
  .popular-posts__carousel-wrapper {
    width: 100%; /* Explizit volle Breite */
    overflow: hidden;
    padding: 0.5rem 0; /* Padding für Schatten-Sichtbarkeit */
    margin: -0.5rem 0; /* Negative Margin um Padding zu kompensieren */
    container-type: inline-size; /* Wrapper als Container für cqw */
  }

  /* Carousel Track - Grid-Spalten mit cqw Units relativ zum Wrapper */
  .popular-posts__carousel {
    --visible-items: 3;
    --gap-size: 0.375rem;

    display: grid;
    grid-auto-flow: column;
    /* cqw bezieht sich jetzt auf carousel-wrapper Breite */
    grid-auto-columns: calc((100cqw - (var(--visible-items) - 1) * var(--gap-size)) / var(--visible-items));
    gap: var(--gap-size);
    transition: transform 0.5s ease-in-out;
    padding-bottom: 0.5rem; /* Extra Padding für Schatten unten */
  }

  /* ==========================================
     CARD STYLES
     ========================================== */

  .popular-posts__card {
    min-width: 0;
    background: white;
    border-radius: 0.5rem;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .popular-posts__card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
  }

  /* Link covers entire card */
  .popular-posts__card-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  /* Image Wrapper */
  .popular-posts__card-image-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: var(--color-muted);
  }

  .popular-posts__card-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .popular-posts__card:hover .popular-posts__card-image {
    transform: scale(1.05);
  }

  /* Content */
  .popular-posts__card-content {
    padding: 0.625rem;
  }

  /* Title */
  .popular-posts__card-title {
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-primary);
    margin: 0 0 0.5rem 0;
    line-height: 1.25;
    min-height: 2.5em; /* 2 Zeilen Höhe für einheitliches Layout */
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .popular-posts__card-link:hover .popular-posts__card-title {
    color: var(--color-accent);
  }

  /* Meta Info Grid */
  .popular-posts__card-meta-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.25rem;
  }

  .popular-posts__card-meta-item {
    display: flex;
    align-items: center;
    gap: 0.2rem;
    font-size: 0.65rem;
    color: var(--color-gray-600);
  }

  .popular-posts__card-icon {
    width: 0.75rem;
    height: 0.75rem;
    flex-shrink: 0;
    color: var(--color-accent);
  }

  .popular-posts__card-meta-text {
    font-weight: 500;
    color: var(--color-gray-700);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    font-size: 0.65rem;
  }

  /* Screen Reader Only */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }

  /* ==========================================
     RESPONSIVE DESIGN mit Container Queries
     Hinweis: Content-Bereich (lg:col-span-2) kann maximal ~790px breit werden
     ========================================== */

  /* Mobile: 1 Karte (bis 400px Container-Breite) */
  @container (max-width: 400px) {
    .popular-posts__carousel {
      --visible-items: 1;
      --gap-size: 0.75rem;
    }

    .popular-posts__nav-button {
      width: 2rem;
      height: 2rem;
    }

    .popular-posts__nav-button--prev {
      left: 0;
    }

    .popular-posts__nav-button--next {
      right: 0;
    }

    .popular-posts__nav-icon {
      width: 1rem;
      height: 1rem;
    }

    .popular-posts__card-content {
      padding: 0.875rem;
    }

    .popular-posts__card-title {
      font-size: 0.9rem;
    }

    .popular-posts__card-meta-grid {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }

    .popular-posts__card-meta-item {
      font-size: 0.75rem;
    }
  }

  /* Tablet: 2 Karten (401px - 650px Container-Breite) */
  @container (min-width: 401px) and (max-width: 650px) {
    .popular-posts__carousel {
      --visible-items: 2;
      --gap-size: 0.5rem;
    }

    .popular-posts__card-title {
      font-size: 0.9rem;
    }
  }

  /* Desktop: 3 Karten (ab 651px Container-Breite) */
  @container (min-width: 651px) {
    .popular-posts__carousel {
      --visible-items: 3;
      --gap-size: 0.375rem;
    }
  }

  /* Fallback für Browser ohne Container Query Support */
  @supports not (container-type: inline-size) {
    @media (max-width: 640px) {
      .popular-posts__carousel {
        --visible-items: 1;
        --gap-size: 0.75rem;
      }

      .popular-posts__card-content {
        padding: 0.875rem;
      }

      .popular-posts__card-title {
        font-size: 0.9rem;
      }

      .popular-posts__nav-button {
        width: 2rem;
        height: 2rem;
      }

      .popular-posts__nav-button--prev {
        left: 0;
      }

      .popular-posts__nav-button--next {
        right: 0;
      }
    }

    @media (min-width: 641px) and (max-width: 1024px) {
      .popular-posts__carousel {
        --visible-items: 2;
        --gap-size: 0.5rem;
      }

      .popular-posts__card-title {
        font-size: 0.9rem;
      }
    }

    @media (min-width: 1025px) {
      .popular-posts__carousel {
        --visible-items: 3;
        --gap-size: 0.375rem;
      }
    }
  }
</style>

<script>
  // Carousel Navigation Logic
  class PopularPostsCarousel {
    private track: HTMLElement;
    private prevButton: HTMLButtonElement;
    private nextButton: HTMLButtonElement;
    private statusElement: HTMLElement | null;
    private currentIndex: number = 0;
    private cardsPerView: number = 3;
    private totalCards: number = 0;

    constructor(container: HTMLElement) {
      this.track = container.querySelector('[data-carousel-track]') as HTMLElement;
      this.prevButton = container.querySelector('[data-carousel-prev]') as HTMLButtonElement;
      this.nextButton = container.querySelector('[data-carousel-next]') as HTMLButtonElement;
      this.statusElement = container.querySelector('[data-carousel-status]');

      if (!this.track || !this.prevButton || !this.nextButton) return;

      this.totalCards = this.track.children.length;
      this.updateCardsPerView();

      // Event Listeners
      this.prevButton.addEventListener('click', () => this.prev());
      this.nextButton.addEventListener('click', () => this.next());

      // Update on resize
      window.addEventListener('resize', () => {
        this.updateCardsPerView();
        this.updatePosition();
      });

      // Initial state
      this.updateButtons();
    }

    private updateCardsPerView() {
      // Verwende die Container-Breite (nicht Viewport), passend zu CSS Container Queries
      const containerWidth = this.track.parentElement?.offsetWidth || 0;

      if (containerWidth <= 400) {
        this.cardsPerView = 1;
      } else if (containerWidth <= 650) {
        this.cardsPerView = 2;
      } else {
        this.cardsPerView = 3;
      }
    }

    private next() {
      const maxIndex = Math.ceil(this.totalCards / this.cardsPerView) - 1;
      if (this.currentIndex < maxIndex) {
        this.currentIndex++;
        this.updatePosition();
      }
    }

    private prev() {
      if (this.currentIndex > 0) {
        this.currentIndex--;
        this.updatePosition();
      }
    }

    private updatePosition() {
      if (this.track.children.length === 0) return;

      const cardWidth = (this.track.children[0] as HTMLElement).offsetWidth;

      // Gap-Größe muss mit CSS Container Queries übereinstimmen
      const containerWidth = this.track.parentElement?.offsetWidth || 0;
      let gap = 6; // Default: 0.375rem

      if (containerWidth <= 400) {
        gap = 12; // 0.75rem für Mobile
      } else if (containerWidth <= 650) {
        gap = 8;  // 0.5rem für Tablet
      }

      const offset = this.currentIndex * this.cardsPerView * (cardWidth + gap);

      this.track.style.transform = `translateX(-${offset}px)`;
      this.updateButtons();
      this.updateStatus();
    }

    private updateButtons() {
      const maxIndex = Math.ceil(this.totalCards / this.cardsPerView) - 1;

      this.prevButton.disabled = this.currentIndex === 0;
      this.nextButton.disabled = this.currentIndex >= maxIndex;
    }

    private updateStatus() {
      if (this.statusElement) {
        const start = this.currentIndex * this.cardsPerView + 1;
        const end = Math.min((this.currentIndex + 1) * this.cardsPerView, this.totalCards);
        this.statusElement.textContent = `Zeige ${start}-${end} von ${this.totalCards} beliebten Rezepten.`;
      }
    }
  }

  // Initialize all carousels on the page
  document.addEventListener('DOMContentLoaded', () => {
    const carousels = document.querySelectorAll('.popular-posts__carousel-container');
    carousels.forEach((carousel) => {
      new PopularPostsCarousel(carousel as HTMLElement);
    });
  });
</script>
